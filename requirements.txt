# Direct dependencies only. A `pip freeze` dump also pins transitive packages
# (pydantic_core, starlette, httpx...), which pins versions nobody chose and
# breaks the build when one of them cannot be resolved for the target platform.
# Let the resolver derive those from the packages actually imported.

fastapi==0.139.0
uvicorn==0.51.0
pydantic==2.13.4

openai==2.45.0
qdrant-client==1.12.1

prometheus_client==0.25.0
prometheus-fastapi-instrumentator==8.0.2
